import React from "react";
import { motion } from "motion/react";

import { pageTransition } from "./../assets/Elements";

const Error = () => {
  return <motion.div {...pageTransition}>Error</motion.div>;
};

export default Error;
